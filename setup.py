from setuptools import setup

setup(name='jpt',
      version='0.1',
      description='JAX perspective transform',
      url='https://github.com/josephrocca/jax-perspective-transform',
      license='Apache 2.0',
      packages=['jpt'],
      zip_safe=False)
